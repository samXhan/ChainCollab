# python.yml
mode: dev  # 或 prod

database:
  mysql:
    host: localhost
    port: 3306
    user: root
    password: password
    dbname: devdb

  redis:
    addr: localhost
    port: 6379
    password: ""
    db: 0

queue:
  type: redis
  redis:
    addr: localhost:6379
    queue_name: ipfs-tasks
    worker_count: 5
